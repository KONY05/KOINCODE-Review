i just had a code review for the KOINCODE CLI and i pushed lots of commits into the pr, like 5 commits and the review came back and I noticed it mentioned a bug that was fixed in the commit already and referenced probably a stale look at the code or something

the image is attached, let's look into it