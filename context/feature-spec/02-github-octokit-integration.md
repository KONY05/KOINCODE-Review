so now i want us to start populating the pages step buy step, we'll start with the `/dashboard` page.

meaning we will have to have functions for: fetching the user's contributions (github activity heatmap, they used octokit graphql), contribution stats for the the stats component and monthly activity

in the tutorial i saw they had to get an access token from github to be able to use octokit, to verify.