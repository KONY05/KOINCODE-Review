# Progress Tracker

Update this file whenever the current phase, active feature, or implementation state changes.

## Current Phase
- AI review pipeline — webhook ingestion, review agent, and comment posting.

## Current Goal
- None — Feature 21 (CLI multi-provider repo support, server side) just shipped, closing the last gap Feature 20's spec flagged as out of scope. Azure DevOps's `vso.hooks_write` question is now resolved live (see Feature 19 note) — not grantable, manual-fallback path confirmed working. No other real gaps currently identified.

## Completed
- Initial Next.js 16 scaffold with TypeScript, Tailwind CSS 4, and pnpm (Create Next App).
- Project context documentation (`CLAUDE.md`, `AGENTS.md`, `context/` files).
- Core dependency installation: Clerk, Drizzle, Neon, Inngest, Pinecone, Octokit, Zod, shadcn/ui.
- Clerk auth setup: `ClerkProvider` in root layout, `proxy.ts` with route protection (direct GitHub OAuth, no sign-in/sign-up pages).
- Database schema: `users`, `repos`, `api_keys`, `reviews` tables defined with Drizzle ORM.
- Drizzle config and migration scripts (`db:generate`, `db:migrate`, `db:studio`).
- Inngest client setup and serve endpoint at `/api/inngest`.
- Environment variable template (`.env.local`) and validation module (`config/env.ts`).
- shadcn/ui initialized with base-nova style, button component, and CSS variables.
- Landing page implemented from Claude Design mockup: split-screen layout with hero (left) and GitHub OAuth login (right). Fonts switched to IBM Plex Sans + JetBrains Mono. Full light/dark mode support via `next-themes` with system preference detection. Custom color palette matching the design (amber accent, cream buttons, muted grays). Theme toggle component added.
- Clerk webhook (`POST /api/webhooks/clerk`): verifies `svix` signature, handles `user.created` event, inserts user into `users` table with `ON CONFLICT DO NOTHING` for idempotency. Extracts `clerkId`, `email`, `name`, `avatarUrl`, `githubUsername` from the webhook payload.
- Collapsible sidebar dashboard layout using shadcn/ui `Sidebar` component: GitHub account card, nav menu (Dashboard, Repositories, Reviews, Settings), user footer with sign-out dropdown. Sticky header with sidebar toggle and breadcrumb. Keyboard shortcut (Cmd+B) to toggle sidebar. Mobile responsive via sheet overlay.
- Dashboard page: stat cards (Total Repos, Commits, PRs, AI Reviews), contribution activity placeholder, activity overview chart placeholder.
- Repository page: fetches user repos from GitHub API via Octokit, paginated with infinite scroll (IntersectionObserver), "All" / "Connected" tab toggle (inline with page heading), debounced search (client-side for All tab, server-side for Connected tab), connect/disconnect with optimistic UI, language badges with colored dots, relative timestamps, empty states for each tab/search combo. Server actions in `lib/actions/repos.ts`, GitHub fetching in `lib/github/repos.ts`.
- Reviews page: fetches reviews from DB with cursor-based infinite scroll (IntersectionObserver), summary stat cards (total, completed, pending, failed), review cards with PR title, repo name, status badge, model, comment count, relative timestamps, review summary preview, and "View on GitHub" link. Server action in `lib/actions/reviews.ts`, client components: `ReviewList` (infinite scroll), `ReviewItem` (data card), `ReviewItemSkeleton` (loading state). Empty state when no reviews exist.
- Settings page: API Keys section with table layout (Provider, Encrypted Key, Model selector, Last Used, Status toggle, Delete). Server-fetches keys from DB with masked encrypted values. Model selector per provider, active/inactive toggle (only one key active at a time), delete action. PROVIDERS config extracted to `config/providers.ts` (shared with onboarding). Server actions in `lib/actions/api-keys.ts`. Repository Memory section with paginated table (25/page, 50 cap) showing repo name, rule text, source (manual/GitHub link), active toggle, created date, delete. "Add Rule" dialog for manual memory creation with repo selector and 280-char textarea. Server actions in `lib/actions/repo-memories.ts`.
- Onboarding flow (`/onboarding`): 3-step form (provider selection → model selection → API key entry). Supports Anthropic, OpenAI, Google, xAI providers. "Activate Code Reviews" saves encrypted key + marks onboarding complete. "Skip for now" marks onboarding complete without adding a key. Redirects to dashboard on completion.
- API key encryption/decryption utilities (`lib/crypto.ts`): AES-256-GCM with versioned envelope format (`v1:iv:tag:ciphertext`).
- Dashboard layout server component checks `hasCompletedOnboarding` flag and redirects to `/onboarding` if not completed.
- Initial indexing on connect: when a repo is connected, an Inngest background job fetches the repo tree (README, config files, top-level source) via GitHub API, generates embeddings via Gemini `gemini-embedding-2`, and upserts them into Pinecone (namespaced by `repo:{repoId}`). Repos schema extended with `indexingStatus` and `disconnectedAt` columns plus a unique constraint on `(user_id, github_id)`. Disconnect is now a soft-delete (`isActive = false`, `disconnectedAt = now()`). A daily Inngest cron job purges embeddings and DB rows for repos disconnected 30+ days. Reconnecting within the grace period skips re-indexing if embeddings already exist. Vector module created: `lib/vector/client.ts` (Pinecone client), `lib/vector/embeddings.ts` (Gemini embedding generation with chunking), `lib/vector/indexing.ts` (orchestrator). GitHub tree fetcher: `lib/github/tree.ts`. Inngest functions: `lib/inngest/functions.ts` (`index-repo`, `cleanup-disconnected-repos`). New env vars: `PINECONE_API_KEY`, `PINECONE_INDEX`, `GOOGLE_GENERATIVE_AI_API_KEY` (optional in env schema). Embeddings use Vercel AI SDK (`ai` + `@ai-sdk/google`).
- GitHub webhook endpoint (`POST /api/webhooks/github`): verifies HMAC-SHA256 signature, handles `pull_request` events (`opened` and `synchronize` actions), skips draft PRs, looks up connected repo by `githubId`, creates a `reviews` row with `pending` status, dispatches `pr/review-requested` Inngest event with PR metadata (number, title, URL, head SHA, branches, repo full name). Webhook auto-installation: `connectRepo` creates a GitHub webhook on the repo and stores the `webhookId`; `disconnectRepo` deletes the webhook and clears `webhookId`. Webhook helpers in `lib/github/webhooks.ts`. New env vars: `APP_URL` (webhook callback base URL), `GITHUB_WEBHOOK_SECRET` (now required).
- AI review agent pipeline (`process-review` Inngest function): full end-to-end review pipeline. Loads user config (API key, provider, model) from DB. Retrieves a fresh GitHub token via Clerk Backend API. Fetches PR diff and changed file list (`lib/github/diff.ts`), full file contents at head SHA (`lib/github/files.ts`), and codebase context from Pinecone (`lib/vector/retrieval.ts`). Builds a structured review prompt (`lib/ai/prompts.ts`) and calls the user's LLM via Vercel AI SDK `generateObject()` with Zod schema enforcement (`lib/ai/review.ts`). LLM provider factory (`lib/ai/providers.ts`) supports Anthropic, OpenAI, Google, and OpenRouter. Posts review comments to GitHub as a single PR review using GitHub's suggestion syntax (`lib/github/comments.ts`). Saves parsed comments and summary to the `reviews` table. Incrementally indexes changed files into Pinecone post-review. New dependencies: `@ai-sdk/anthropic`, `@ai-sdk/openai`.
- API key usage logs (Feature 08): `key_usage_logs` table with `ON DELETE SET NULL` for audit persistence. `runReview()` and `extractRule()` return usage/duration metadata; `generateEmbeddings()` returns tokens/duration. All Inngest functions (`process-review`, `process-comment-reply`, `index-repo`, `index-changed-files`) insert log rows after each LLM/embedding call with try/catch to never break the pipeline. Logs page at `/logs` with summary cards (total calls, total tokens, success rate, avg duration), filterable table (action, status, repo, date range), cursor-based pagination (25 rows), token breakdown tooltips, and status indicators. "Logs" nav item added to sidebar.
- Repository memory from PR comment replies: users reply to KOINCODE review comments on GitHub to teach the agent repo-specific conventions. Webhook subscribes to `pull_request_review_comment` events, detects replies to KOINCODE-authored comments, dispatches `comment/reply-received` Inngest event. `processCommentReply` function extracts a rule via the user's LLM, deduplicates against existing memories, stores in `repo_memories` table, and confirms on GitHub. Max 50 active memories per repo; rules capped at 280 chars. `processReview` loads active repo memories and injects them into the review prompt as "Repository Rules" section. Schema: `repo_memories` table (`lib/db/schema/repo-memories.ts`). Rule extraction: `lib/ai/memory.ts`. GitHub reply helper: `replyToComment` in `lib/github/comments.ts`.
- **CLI Integration — device auth + repo connect API (Feature 14)**: exposes a token-based API surface so the separate KOINCODE terminal app can authenticate as a Review user and connect/disconnect repos without a browser session. Browser-based device-authorization flow (`POST /api/cli/device` → CLI opens `/cli-auth?device_code=...` → user signs in via existing Clerk GitHub OAuth if needed and clicks Allow/Deny → CLI polls `POST /api/cli/device/token` and receives a long-lived CLI token). Two new Postgres tables (Drizzle, not in-memory — this app runs on Vercel, where separate requests in this flow have no guarantee of hitting the same function instance): `cli_tokens` (hashed tokens, sha256) and `pending_device_auth` (short-lived, ~5 min TTL, opportunistic cleanup on insert rather than a dedicated cron). New `lib/cli/device-auth.ts` (token minting/hashing, pending-auth queries), `lib/cli-auth.ts` (`requireCliToken()` Bearer-auth guard, joins straight to `users` for `clerkId`). Bearer-authed routes: `POST /api/cli/repos/connect`, `POST /api/cli/repos/disconnect`, `GET /api/cli/repos/status` — all looked up by GitHub `owner/repo` full name rather than the internal repo id, since that's all a git remote gives the CLI. Connect/disconnect logic extracted from `lib/actions/repos.ts` into a shared, non-"use server" `lib/repos/connect.ts` (`connectRepoForUser`/`disconnectRepoForUser`/`findConnectedRepoGithubId`/`getRepoStatusForUser`) so the dashboard's server actions and the new CLI routes run the exact same webhook/DB/indexing tail — deliberately not exported from a `"use server"` file, so it can't become an accidentally client-callable Server Action. `getGithubTokenForClerkUser(clerkId)` added alongside the existing session-based `getGithubToken()` in `lib/github/index.ts`; `fetchRepoByFullName()` added to `lib/github/repos.ts` for resolving a single owner/repo (the CLI has no pre-fetched repo list to match against, unlike the dashboard flow). `GitHubSignInButton` gained an optional `redirectUrlComplete` prop so `/cli-auth` can loop the OAuth redirect back to itself (with `device_code` intact) instead of losing that context to the hardcoded `/dashboard` default. `proxy.ts`: `/cli-auth` and `/api/cli(.*)` added to public routes (the page handles its own signed-in/signed-out branching like `/` already does; the API routes use Bearer-token auth instead of a Clerk session). Migration generated (`drizzle/0007_goofy_boom_boom.sql`) and applied — `pnpm db:migrate` run against the live Neon dev DB. Verified via `pnpm typecheck`, `pnpm lint`, and `next build` (all clean); the device-auth flow itself has not been exercised live (no CLI-side testing yet — see the CLI-side spec). CLI-side commands, and the reasoning behind the two-repo split, are in `KOINCODE/context/feature-specs/42-koincode-review-integration.md`. Spec: `context/feature-spec/14-cli-integration-auth.md`.

- **CLI Integration — key/model sync (Feature 15)**: new Bearer-authed `POST /api/cli/keys/sync` route so the KOINCODE CLI can push its currently active provider API key + model into a user's `api_keys` row, the same table the Settings page's "API Keys" section manages — a second entry point onto existing storage, no new activation model. Request body `{ provider, model, apiKey }` validated by a new `keySyncSchema` in `lib/cli/schemas.ts` (`provider` as a `z.enum` matching `LlmProvider` exactly). `model` is additionally rejected unless it's in that provider's list from `config/providers.ts`'s `getProviderConfig(provider).models` — the same canonical list the Settings/onboarding model selector already draws from, so a model id the web UI's own dropdown wouldn't offer can never be written via this path. New `lib/cli/keys.ts`'s `syncApiKeyForUser(userId, provider, model, apiKey)` — not a reuse of the existing `addApiKey` (insert-only, would collide with the `(userId, provider)` unique constraint on a second sync) — instead a Drizzle `onConflictDoUpdate` upsert on that same constraint: `isDefault` is set `true` only on the insert branch and only when the user had zero key rows before (mirrors `addApiKey`'s `isDefault: isFirstKey`), and is omitted entirely from the update branch's `set`, so refreshing a previously-synced provider's key never touches whichever key is currently active — the concrete implementation of "additive, never overriding." Reuses `EVENTS.API_KEY_ADDED` with `source: "cli"` rather than a new event constant, same pattern as `REPO_CONNECTED`/`REPO_DISCONNECTED` in Feature 14. No `proxy.ts` change needed — `/api/cli(.*)` was already made public in Feature 14, and this route does its own `requireCliToken()` Bearer check like the repos routes. Verified via `pnpm tsc --noEmit` and `pnpm lint` (both clean); not yet exercised live — depends on the CLI-side `/review-sync-keys` command (`KOINCODE/context/feature-specs/42-koincode-review-integration.md`'s "Phase 2" section) to actually call it. Spec: `context/feature-spec/15-cli-key-sync.md`.

- **Git Provider Abstraction (Feature 17)**: pure refactor, zero user-visible behavior change — introduces a `GitProvider` interface (`lib/providers/types.ts`) that GitHub now implements (`lib/providers/github/*`, replacing `lib/github/*`), so GitLab (Feature 18) and Azure DevOps (Feature 19) can plug in without touching the review pipeline again. Schema: `repos.github_id` (integer) → `repos.external_id` (text) + new `provider` enum column (`git_provider`, currently only `"github"`) + `webhook_id` widened to text; unique constraint moved to `(user_id, provider, external_id)`. Migrated in two steps (`drizzle/0008_provider-abstraction-step1.sql`, `0009_provider-abstraction-step2.sql`) to dodge drizzle-kit's interactive rename prompt (no TTY available); backfilled and verified against the live dev DB with zero data loss (1 repo row, 24 review rows intact). `lib/repos/index.ts`'s shared connect/disconnect tail now dispatches webhook create/delete through `lib/providers/registry.ts`'s `getProvider()`. Deliberately **not** rewired: `lib/inngest/functions.ts` (the actual review pipeline) still calls GitHub functions directly and hardcodes `"github"` token lookups — dynamic provider dispatch there is Feature 18's job, once a second provider's actual needs are known, not guessed at now. `reviews.comments[].githubCommentId` field name/type also deliberately kept as-is (naming call deferred to Feature 18). Verified via `pnpm tsc --noEmit`, `pnpm lint`, `pnpm build` (all clean). Spec: `context/feature-spec/17-git-provider-abstraction.md`.

- **GitLab Integration (Feature 18)**: GitLab is now a real second `GitProvider` (`lib/providers/gitlab/*`) — repo listing, webhook create/delete, tree/file fetching, MR diffs, threaded Discussions-based inline comments (with the `DiffNote`-vs-plain-note silent-downgrade check GitLab's API needs), and commit statuses (mapped to GitLab's different state vocabulary). New `app/api/webhooks/gitlab/route.ts` handles `merge_request`/`note` events with GitLab's plaintext `X-Gitlab-Token` auth (no HMAC). `lib/inngest/functions.ts` — deliberately left hardcoded to GitHub in Feature 17 — is now actually provider-aware: every function resolves the repo's `provider` column fresh and dispatches through `getProvider()` instead of importing GitHub functions directly. `reviews.comments[].githubCommentId: number` renamed to `providerCommentId: string` (GitLab's discussion ids are string hashes, not numbers) — the naming call Feature 17 explicitly deferred. Extracted genuinely shared utilities now that there's a 2nd consumer: `lib/providers/diff-utils.ts` (hunk parsing, file-skip patterns), `lib/providers/tree-classify.ts` (README/config detection), `lib/providers/review-body.ts` (markdown construction — GitLab supports GitHub's identical ` ```suggestion ` fence syntax), and `lib/webhooks/repo-lookup.ts` (repo/review lookups shared between the GitHub and GitLab webhook routes; payload parsing and event-dispatch orchestration deliberately stay separate per route). Settings > Connections gained a "Connect GitLab" account-linking flow (`components/Settings/connections/GitProviderConnections.tsx`) using Clerk's `user.createExternalAccount()` to link GitLab onto the existing signed-in identity rather than creating a separate account — reuses the existing `/sso-callback` route, with a `?intent=link` flag added so linking doesn't get double-counted as a sign-in in analytics. **Deliberately not built here**: any UI to actually connect a GitLab *repo* (Feature 20's job — see below) or CLI support for GitLab. **Needs manual setup to test live**: a GitLab OAuth application registered and enabled as a Clerk social connection — not something doable from code. Verified via `pnpm tsc --noEmit`, `pnpm lint`, `pnpm build` (all clean); nothing exercised against a real GitLab instance yet, especially the reply-detection heuristic (GitLab has no equivalent of GitHub's `in_reply_to_id`). Spec: `context/feature-spec/18-gitlab-integration.md`.

- **Azure DevOps Integration (Feature 19)**: Azure DevOps is now a third `GitProvider` (`lib/providers/azure-devops/*`), auth via Clerk's `oauth_microsoft` strategy + `getTokenForClerkUser`. Schema: `git_provider` enum gained `"azure_devops"`; new nullable `repos.webhookSecret` column for the manual webhook-setup fallback (migration `drizzle/0012_add-azure-devops-provider.sql`, applied to the dev DB). `GitProvider.createRepoWebhook` is now a hybrid contract (`CreateWebhookResult = {status:"created", webhookId} | {status:"manual", secret}`) since Microsoft's docs mark `vso.hooks_write` "no longer public" — GitHub/GitLab mechanically updated to always return `{status:"created"}`, no behavior change. New `AZURE_DEVOPS_WEBHOOK_SECRET` env var (optional, parallel to `GITLAB_WEBHOOK_SECRET`) used as the Basic Auth password for auto-created service hook subscriptions; the per-repo `webhookSecret` is only populated on the manual-fallback path. Fixed the 3-level org/project/repo hierarchy mismatch by adding `lib/providers/repo-full-name.ts`'s `splitRepoFullName()` (splits on the *last* `/`, which works uniformly for GitHub/GitLab's 2-segment and Azure DevOps's 3-segment full names) and using it at all 5 `repoFullName.split("/")` call sites in `lib/inngest/functions.ts`. New `app/api/webhooks/azure-devops/route.ts` verifies deliveries via Basic Auth (not HMAC — Azure DevOps service hooks have no signature header), which means (unlike GitHub/GitLab) verification must happen *after* the repo lookup since the expected secret is per-repo. **Real gaps discovered during implementation, beyond what the spec anticipated**: (1) Azure DevOps has no API that returns unified-diff patch text (unlike GitHub's raw diff / GitLab's per-file `diff` field) — added the `diff` npm package (jsdiff) and a new `lib/providers/azure-devops/local-diff.ts` to compute unified diffs locally by fetching both file versions and diffing them, used by `fetchPRFiles`/`fetchPRDiff`/`fetchPushChanges`; this also means `fetchPRFiles` and `fetchPRDiff` redundantly redo the same fetch+diff work when called together (documented in `diff.ts`, not solved). (2) No flat "list all my repos" endpoint exists — `listUserRepos` fans out via the `vssps.visualstudio.com` profile/accounts endpoints (a different host) to enumerate organizations, then projects, then repos, and paginates via an in-memory slice rather than a true page token. (3) `git.pullrequest.updated`'s payload has no before/after commit shas, so it can't be distinguished from any other PR metadata edit — **re-review-on-new-commits and adoption tracking are not wired for Azure DevOps in this pass**; only initial review-on-creation and cancel-on-merge/close work. (4) Azure DevOps has no one-click "Apply suggestion" UI, so `formatCommentBody`'s ` ```suggestion ` fence just renders as a plain code block there. **Deliberately not built here**: the connect-repo UI (Feature 20), including surfacing the manual webhook-setup secret to the user (`connectRepoForUser` returns it as `manualWebhookSecret`, unused by any caller yet — same "not wired to the CLI/dashboard connect flow yet" position GitLab was left in after Feature 18). **Live-verified, resolved**: connecting a real Azure DevOps repo confirmed `vso.hooks_write` is *not* grantable to the registered Entra app — `createRepoWebhook` correctly fell through to the `{status:"manual"}` branch and the connect flow surfaced the copy-to-clipboard secret + manual setup instructions as designed (Feature 20's `AzureWebhookSecretDialog`). The hybrid contract's "manual" path is real, exercised behavior now, not a hypothetical. Verified via `pnpm tsc --noEmit`, `pnpm lint`, `pnpm build`, the migration applied cleanly to the dev DB, and this live manual-fallback test. Spec: `context/feature-spec/19-azure-devops-integration.md`.

- **Multi-Provider UI (Feature 20)**: the three remaining GitHub-hardcoded UI surfaces are now multi-provider. (1) **Repos page** (`lib/actions/repos.ts`, `RepoList.tsx`) — `listGithubRepos` became `listProviderRepos(provider, ...)`, resolving tokens via `getProvider(provider)` instead of a GitHub-only helper; a `ProviderSelector` (tabs, only rendered when a user has linked more than one provider) drives which provider's repos the "All" tab lists, persisted in the URL (`?provider=`); `connectRepo`/`disconnectRepo` now take a `provider` argument. Azure DevOps's manual-webhook-secret fallback (Feature 19) is now actually surfaced: `connectRepo` returns `manualWebhookSecret`/`webhookUrl` when the auto-create path 403'd, and `AzureWebhookSecretDialog` shows it with copy-to-clipboard plus the real Azure DevOps navigation path (Project Settings → Service Hooks → Web Hooks, Basic auth). (2) **Dashboard** — the GitHub-only contribution calendar/stats (`GithubActivity.tsx`, `lib/providers/github/contributions.ts`, both now deleted) are replaced with data sourced entirely from our own `reviews` table, which works identically regardless of provider: `ReviewActivity.tsx` (daily review-count calendar, quartile levels computed locally instead of received from GitHub's API), `Stats.tsx` (Connected Repos / Reviews Completed / Issues Found / Adoption Rate), and `ActivityOverview.tsx` (monthly Reviews vs. Issues Found, replacing Commits/PRs/Reviews). New `lib/actions/reviews.ts` exports: `fetchDailyReviewCounts`, `fetchDashboardStats`; `fetchMonthlyReviewCounts` reworked to aggregate in JS (needed for `issuesFound`, not a plain SQL `count()`). (3) **Landing page** (`app/page.tsx`) — the single "Continue with GitHub" button is now `ProviderSignInButtons`, rendering GitHub/GitLab/Azure DevOps from a new shared `lib/providers/clerk-mapping.ts` (`PROVIDER_CLERK_CONFIG`, also now used by `GitProviderConnections.tsx`'s linking UI and the repos page's provider detection, replacing three independent copies of the same github/gitlab/microsoft mapping). **Real constraint hit while building this**: Clerk's `signIn.authenticateWithRedirect()` has no `additionalScopes` parameter at all (checked against Clerk's own types) — unlike `createExternalAccount()` (the linking flow), so a brand-new GitLab/Azure DevOps-first signup's OAuth scopes depend entirely on that connection's dashboard-configured default "Scopes" field, with no code-level request possible at first sign-in. `app/cli-auth/page.tsx` deliberately keeps using the old GitHub-only `GitHubSignInButton` — the CLI integration is still explicitly GitHub-only (see Open Questions). GitLab's/Azure DevOps's sign-in buttons use generic lucide icons rather than hand-drawn brand SVGs (no `simple-icons`-style package installed; reproducing exact brand marks from memory risked shipping a wrong one). `CLAUDE.md` updated to match (auth bullet, `lib/providers/` replacing the stale `lib/github/` reference, env var list). Verified via `pnpm tsc --noEmit`, `pnpm lint`, `pnpm build` (all clean); nothing exercised in a live browser session yet. Spec: `context/feature-spec/20-multi-provider-ui.md`.

- **CLI Multi-Provider Repo Support, server side (Feature 21)**: closes the gap Feature 20 explicitly deferred — `/api/cli/repos/*` was still 100% GitHub-only even after Features 17–20 made the rest of the app provider-aware. `repoRefSchema` (`lib/cli/schemas.ts`) gained `provider: z.enum(["github","gitlab","azure_devops"]).default("github")` — optional-with-default so already-installed CLI binaries (which send no `provider` field) keep working unmodified; `owner`'s max length widened from GitHub's 39-char username limit to 100, since Azure DevOps's `owner` is the combined `"organization/project"` string. Real bug fix, not just a feature addition: `findConnectedRepoExternalId` and `getRepoStatusForUser` (`lib/repos/index.ts`) previously looked up by `(userId, owner, name)` with **no provider filter** — a user with a GitHub repo and a GitLab repo sharing the same `(owner, name)` (plausible for a mirrored repo) would have silently matched whichever row came back first; both now take a `provider` parameter and filter on it. All three routes (`app/api/cli/repos/{connect,disconnect,status}/route.ts`) swapped their hardcoded `githubProvider` import for `getProvider(parsed.data.provider)` (registry-based dispatch, the same pattern `lib/actions/repos.ts` already used post-Feature-20) and thread `provider` through to `connectRepoForUser`/`disconnectRepoForUser`/`findConnectedRepoExternalId`/`getRepoStatusForUser`. Connect's response gained a `provider` field alongside the existing `{owner, name, fullName}` (harmless addition — the CLI's Zod schema isn't `.strict()`). No schema/migration changes needed — `repos.provider` already existed from Feature 17. Verified via `pnpm tsc --noEmit` (clean). **Not yet verified live** — depends on the CLI-side half (spec 44 in the KOINCODE repo, on its own `feature/multi-provider-support` branch) sending the new `provider` field; also not yet tested against an already-installed old-shape request to confirm the default actually protects it. Cross-repo spec: `KOINCODE/context/feature-specs/44-cli-multi-provider-repo-support.md`. Spec: `context/feature-spec/21-cli-multi-provider-repo-support.md`.

- **Preview-deployment webhook URL fix (found auditing Feature 21's related surfaces)**: all three providers' `getWebhookUrl()` (`lib/providers/{github,gitlab,azure-devops}/webhooks.ts`) and `lib/actions/repos.ts`'s Azure DevOps manual-fallback `webhookUrl` display were building the callback URL from the static `env.APP_URL` — fine for Production and local dev, but wrong for Preview deployments now that Preview runs against its own Neon branch DB (not production's): a repo connected from a Preview deployment would get a webhook registered against *production's* URL, whose route handler queries *production's* DB, finds nothing, and silently no-ops — meaning the connect→webhook→review pipeline could never be exercised end-to-end on Preview, with no error surfaced anywhere. Fixed with a new `getAppUrl()` (`config/env.ts`): returns `https://${VERCEL_URL}` when `VERCEL_ENV === "preview"` (both auto-injected by Vercel, no config needed), otherwise falls back to the static `env.APP_URL`. Ports forward a fix already present on the older `development` branch (same function, same logic) that predates the Feature 17 provider-abstraction refactor and so only ever touched the single old `lib/github/webhooks.ts` — this pass re-applies it across all three post-refactor provider webhook files plus the Azure DevOps manual-fallback display, which didn't exist yet when `development`'s version was written. Deliberately left untouched, matching `development`'s precedent: `app/api/cli/device/route.ts`'s CLI device-auth `verificationUrl` and `app/layout.tsx`'s `metadataBase` — neither is a webhook callback, so pinning to the stable production URL is correct for both. Verified via `pnpm tsc --noEmit` (clean); not yet verified against a live Preview deployment.

## In Progress
- None.

## Recently Shipped (not yet in Completed)
- Review Quality Adoption Tracking (Feature 12): passive analytics to measure whether AI review comments are actually useful. On `pull_request.synchronize`, dispatches `pr/adoption-check` Inngest event with `before`/`after` SHAs. `trackAdoption` function fetches the compare diff via GitHub API, parses hunk ranges, and checks if any completed review comments (file path + line within 5-line proximity) were modified in the new push — marking them as `applied`. On PR merge, remaining `pending` comments are marked `resolved` (ignored). Fires `review_adoption_detected` Mixpanel event per review with `adopted_count`, `pending_count`, and `total_comments` properties. New files: `lib/github/adoption.ts` (fetchPushChanges, detectAdoptions). Modified: webhook handler (PullRequestPayload type, synchronize handler, merge handler), Inngest functions (trackAdoption), Inngest serve route (registration), analytics events.

## Next Up
- None currently queued. Both prior candidates resolved: `vso.hooks_write` verified against a live Azure DevOps org (not grantable, manual fallback confirmed working), and the CLI integration is now provider-qualified (Feature 21).

## Recently Shipped (not yet in Completed)
- Review Head-SHA Consistency & Late-Cancellation Guard (Feature 13): `run-review` now resolves a fresh `reviewSha` via `fetchPRHeadSha` (new helper in `lib/github/diff.ts`, a plain `pulls.get` call) alongside `fetchPRFiles`/`fetchPRDiff`, instead of trusting the webhook-time `headSha`. `reviewSha` is used for `fetchChangedFileContents`, the success commit status, `postReviewComments`, and the `repo/index-changed-files` dispatch, so diff/files/contents/posted-comment commit are all guaranteed to describe the same commit. Added a second `isReviewStillActive` check (`check-still-active-before-post` step) immediately before `postReviewComments`, closing the window the original Feature 10 check (pre-fetch only) left open through the LLM call. `postReviewComments` (`lib/github/comments.ts`) now drops a suggestion (keeps the prose body) via new `extractPatchLines`/`isNoOpSuggestion` helpers when the suggested code is identical to what it would replace. `reviewResponseSchema` (`lib/ai/review.ts`) gained `.describe()` on `startLine`/`line`/`body`/`suggestion` to reinforce the line-range and no-duplication rules at the schema level, not just in the system prompt.

## Recently Completed
- Error Tracking & Product Analytics (Feature 11): Sentry (`@sentry/nextjs`) for full error tracking — client/server/edge configs, `instrumentation.ts` with `onRequestError`, `instrumentation-client.ts`, `global-error.tsx` with `captureException()`, `withSentryConfig()` in `next.config.ts` for source maps and performance monitoring, `Sentry.captureException()` in Inngest function catch blocks. Mixpanel for product analytics — `lib/analytics/events.ts` (27 event constants), `lib/analytics/mixpanel.ts` (client + server wrappers), `AnalyticsProvider` component in dashboard layout (init, identify, page view tracking). Server-side `trackServer()` calls in all server actions (`repos`, `api-keys`, `onboarding`, `repo-memories`) and Inngest functions (`indexRepo`, `processReview`, `processCommentReply`). Webhook handler tracks `review_requested` and `review_cancelled`. Env vars: `NEXT_PUBLIC_SENTRY_DSN`, `SENTRY_ORG`, `SENTRY_PROJECT`, `SENTRY_AUTH_TOKEN`, `NEXT_PUBLIC_MIXPANEL_TOKEN`.
- Cancel In-Flight Reviews on PR Close/Merge (Feature 10): webhook handles `closed` action — finds `pending`/`in_progress` reviews for the PR, marks them `failed` with descriptive summary ("PR was merged" or "PR was closed"), dispatches `pr/review-cancelled` Inngest event. `cancelReview` function sets commit status to `success` with "Review cancelled — PR closed" (GitHub statuses don't support `neutral`, `success` is the least-alarming equivalent). `synchronize` action now supersedes in-flight reviews before creating the new one ("Superseded by newer commit."). `processReview` checks `isReviewStillActive` before the expensive `run-review` step — bails out early if the review was cancelled by the webhook. Shared `cancelInFlightReviews` helper for both `closed` and `synchronize` paths.
- GitHub Commit Statuses (Feature 09): `lib/github/checks.ts` with `createCommitStatus` helper using GitHub's Commit Statuses API (works with OAuth tokens, unlike the Checks API which requires a GitHub App). `processReview` Inngest function restructured — GitHub token is now fetched before the API key check so the status can be set early. Pipeline wrapped in try/catch so commit status is always updated (pending → success/failure/error). No-API-key early exit now sets a failure status and returns.

## Open Questions
- ~~Whether Azure DevOps's `vso.hooks_write` is actually grantable~~ — resolved (Feature 19 update above): not grantable, manual-webhook-secret fallback confirmed working live.
- ~~The CLI integration is GitHub-only~~ — resolved by Feature 21: `/api/cli/repos/*` is now provider-qualified.

## Architecture Decisions
- Next.js 16 App Router with Server Components for data fetching and Server Actions for mutations.
- Clerk for auth — handles GitHub OAuth, session management, and user metadata.
- Drizzle ORM over Neon (serverless Postgres) — type-safe queries with migration support.
- Inngest for background jobs — event-driven, with built-in retries and observability.
- Pinecone for vector storage — used to provide codebase context to the review agent.
- BYOK (bring your own key) model — no built-in LLM hosting. Users provide API keys for their preferred provider.
- Octokit for all GitHub API operations — webhooks, PR reads, review comments, commits.
- Next.js 16 uses `proxy.ts` instead of `middleware.ts` — Clerk's `clerkMiddleware` is exported as the default from `proxy.ts`.
- Zod v4 (`zod/v4` import path) for runtime validation at API boundaries.
- AES-256-GCM for encrypting user API keys at rest. Stored as versioned envelope: `v{n}:iv:tag:ciphertext`. Supports key rotation via re-encryption migration. Decrypt only inside background jobs at moment of LLM invocation.
- All DB primary keys use `uuid().defaultRandom()` — Postgres-generated, no app-side ID creation.
- Review comments include optional `suggestedDiff` (`{ oldCode, newCode }`) for rendering before/after diffs alongside the "Apply Fix" action.
- Hybrid vector indexing: lightweight initial index on connect (tree structure, README, config files, top-level source), then incrementally index full file content as files appear in PRs. Vector store fills naturally with code that actually changes.
- Embedding model strategy: if the user has a Google/Gemini API key, use their key for embeddings. Otherwise, fall back to the platform-owned Gemini key (`gemini-embedding-2`, free tier). Saves platform quota when possible.
- Auth is direct GitHub OAuth via Clerk — no sign-in/sign-up pages, just a login button that triggers OAuth.

## Session Notes
- Next.js version is 16.2.9 — has breaking changes from prior versions. Always check `node_modules/next/dist/docs/` before using Next.js APIs.
- Key Next.js 16 breaking changes: `params`, `searchParams`, `cookies`, `headers` are all async (must `await`). `middleware.ts` → `proxy.ts`. Turbopack is default.
- Package manager is pnpm.
- Tailwind CSS version is 4 (PostCSS plugin via `@tailwindcss/postcss`).
- Path alias `@/` is configured in tsconfig.json.
- shadcn/ui uses base-nova style with neutral base color and lucide icons.
